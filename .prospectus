item do
  expected do
    url_xpath
    url 'http://libarchive.org/'
    xpath '//*[@id="container"]/ul[1]/li[1]/a[1]'
    regex /^libarchive-([\d.]+).tar.gz$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
